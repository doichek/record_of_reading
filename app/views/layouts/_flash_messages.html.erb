<% flash.each do |message_type, message| %>
  <div  style="padding-bottom:4px; padding-top:4px; font-size:12px; font-weight:bold" class="alert alert-<%= message_type %>"><%= message %></div>
<% end %>